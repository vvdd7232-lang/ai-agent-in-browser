# ai-agent-in-browser

**Обычный ИИ-чат в браузере + твой локальный терминал = агент, который реально работает.**

ChatGPT / Claude / Gemini в браузере не могут ничего, кроме текста. Этот проект замыкает цикл:
программа-мост даёт модели настоящий терминал, а расширение читает её ответы, выполняет блоки
`[EXECUTE]` и возвращает вывод обратно в чат. Модель видит результат — и продолжает сама, шаг за
шагом, пока задача не сделана.

```
   ┌────────────────────────── браузер ──────────────────────────┐
   │  ChatGPT / Claude / Gemini / любой чат                       │
   │                                                              │
   │  ИИ пишет:            расширение:                            │
   │  ```bash               1. находит [EXECUTE] в ответе         │
   │  # [EXECUTE]           2. шлёт команду мосту                 │
   │  npm init -y           3. ждёт вывод                         │
   │  ```                   4. вставляет [TERMINAL] в поле ввода  │
   │        ▲               5. жмёт «отправить»                   │
   │        │                                                     │
   │        └────────────── модель читает вывод и идёт дальше     │
   └───────────────────────────────┬──────────────────────────────┘
                                   │  HTTP + WS (127.0.0.1, токен)
   ┌───────────────────────────────▼──────────────────────────────┐
   │  мост (node bin/cli.js)                                      │
   │   • живая shell-сессия: cd, файлы и переменные сохраняются   │
   │   • режимы auto / confirm / off + эвристика опасных команд   │
   │   • панель: чат с ИИ, живой терминал, промпт, настройки      │
   └───────────────────────────────┬──────────────────────────────┘
                                   ▼
                        bash / zsh / PowerShell — твой компьютер
```

Ноль runtime-зависимостей: только Node.js ≥ 18. HTTP-сервер, WebSocket-сервер и PNG-иконки
собраны вручную.

---

## Быстрый старт

```bash
# 1. запусти мост
node bin/cli.js
```

Консоль напечатает ссылку на панель и **токен**:

```
● Панель  http://127.0.0.1:7788/?token=XXXX
● Токен   XXXX
```

1. Открой панель по ссылке. В блоке **«1 · Системный промпт»** нажми *Скопировать промпт*.
2. Вставь промпт **первым сообщением** в чат ИИ (ChatGPT / Claude / Gemini). Без него модель
   ответит «я всего лишь ИИ и не имею доступа к компьютеру».
3. Загрузи расширение: `chrome://extensions` → *Developer mode* → *Load unpacked* → выбери папку
   `extension/`. В popup вставь тот же токен, включи тумблер.
4. Поставь задачу в чате. Дальше цикл идёт сам: модель → `[EXECUTE]` → терминал → вывод → модель.

> Хочешь посмотреть цикл без расширения и без ключа API? Открой панель и напиши задачу в
> **«Чат с ИИ»** — там работает демо-ассистент (заглушка модели), но терминал при этом **настоящий**.
> Если задать свой OpenAI-совместимый ключ — чат в панели станет полноценным агентом.

## Структура

```
bin/cli.js            точка входа моста (CLI, баннер, автозапуск)
bridge/server.js      HTTP API, роутинг, auth по токену, SSE-чат
bridge/shell.js       живая shell-сессия + корректное прерывание (без tty!)
bridge/parser.js      разбор [EXECUTE] / ANSI / обрезка вывода (общий для Node и браузера)
bridge/prompt.js      системный промпт (подставляет ОС, shell, cwd)
bridge/llm.js         необязательный LLM-клиент + демо-ассистент
bridge/open.js        кроссплатформенное открытие панели в браузере
bridge/ws.js          минимальный WebSocket-сервер (RFC 6455)
bridge/store.js       конфиг + история (~/.ai-agent-in-browser/)
web/                  панель управления (без сборки)
extension/            Chrome MV3 расширение (content + background + popup)
scripts/              генерация иконок и синхронизация parser.js в расширение
tests/                node:test (парсер, shell, HTTP, панель)
```

## Как мост выполняет команды

* Один shell-процесс на всё время — поэтому `cd`, созданные файлы и переменные **живут между
  командами**, как в настоящем терминале.
* После каждой команды мост читает внутренний маркер с `exit code` и `pwd`, чтобы вернуть модели
  точный результат и не потерять смену каталога.
* **Прерывание без tty.** `Ctrl-C` в stdin не работает (нет терминала), поэтому по таймауту мост
  находит потомков shell через `/proc` (Linux) или `pgrep` (macOS) и убивает их, не трогая сессию.
  Поток после убийства сливается до resync-маркера, чтобы хвост старой команды не попал в новую.
* Вывод чистится от ANSI-кодов и обрезается (голова+хвост) под контекст модели; полный вывод виден
  в терминале панели.

## Безопасность

Мост **выполняет код на твоём компьютере**, поэтому:

* по умолчанию слушает только `127.0.0.1`;
* каждый запрос защищён случайным токеном (`x-agent-token`), токен не отдаётся наружу;
* `confirm`-режим держит команду до твоего «Разрешить» в панели;
* даже в `auto` заведомо разрушительные команды (`rm -rf /`, `curl|sh`, `mkfs`, `git push --force`, …)
  требуют подтверждения;
* если всё же слушаешь `--host 0.0.0.0`, мост честно предупредит в консоли.

Конфиг и история лежат в `~/.ai-agent-in-browser/`. Токен можно сменить: панель или
`POST /api/token/rotate`.

## CLI

```
node bin/cli.js [--port 7788] [--host 127.0.0.1] [--cwd <путь>]
                [--token <секрет>] [--approval auto|confirm|off]
                [--data-dir <путь>] [--no-open]
```

## HTTP API (кратко)

| Метод и путь | Что делает |
| --- | --- |
| `GET /api/health` | статус без токена |
| `GET /api/state` | конфиг (без секрета), shell, статистика, история |
| `GET /api/prompt` | системный промпт с подставленной ОС |
| `POST /api/execute` | выполнить команду → `{id, status}` |
| `GET /api/result/:id?wait=…` | long-poll результата + готовый `[TERMINAL]`-ответ |
| `POST /api/decision` | разрешить/отклонить команду в confirm-режиме |
| `POST /api/abort` / `/api/reset` | прервать команду / перезапустить shell |
| `POST /api/cd` / `/api/config` | сменить каталог / обновить настройки |
| `POST /api/chat` | SSE-чат (LLM или демо-ассистент) |
| `POST /api/parse` | разбор произвольного ответа модели (отладка адаптеров) |
| `GET /api/ws` | live-лог терминала (WebSocket) |

## Разработка

```bash
npm test                # 52 теста: парсер + shell + HTTP + панель + CLI
npm run build:ext       # пересоздать extension/parser.js и иконки
node bin/cli.js         # авто-синхронизирует parser.js в расширение при старте
```

## Сборка в один файл (.exe)

Мост можно собрать в ОДИН исполняемый файл, которому не нужен установленный Node —
двойной клик, и панель открыта:

```bash
npm i -D esbuild postject     # только для сборки, для работы не нужны
npm run build:exe             # -> dist/ai-agent-in-browser.exe   (Windows x64)
npm run build:exe:linux       # -> dist/ai-agent-in-browser-linux (Linux x64)
```

Это Node SEA (single executable application): esbuild собирает проект в один бандл,
`node --experimental-sea-config` зашивает туда же панель (`web/`) и копию парсера,
postject внедряет блоб в копию node-бинарника. Бинарник качается из npm-пакетов
`node-win-x64` / `node-linux-x64`, поэтому **.exe собирается из-под любой ОС** —
Windows для сборки не нужен. Перед внедрением скрипт снимает подпись node.exe
(аналог `signtool remove-signature`): иначе Windows видит битую подпись.

Внутри такого файла нет ни `web/`, ни `bridge/` — их читает `bridge/assets.js`,
который прозрачно работает и с диском, и с SEA-ассетами.

> Расширение в .exe не упаковывается: Chrome с версии 137 запретил устанавливать
> расширения иначе как вручную (`chrome://extensions` → *Load unpacked*), флага
> `--load-extension` больше нет. Поэтому папку `extension/` по-прежнему нужно
> загрузить один раз руками.

## Дорожная карта

- [x] живая shell-сессия, прерывание без tty, ANSI/обрезка
- [x] расширение: авто-цикл на ChatGPT/Claude/Gemini + универсальный режим
- [x] панель с демо-агентом и опциональным LLM
- [ ] авто-обновление адаптеров под новые вёрстки чатов
- [ ] Electron-обёртка «поставил и забыл»
- [ ] журнал сессий и diff-просмотр изменённых файлов
