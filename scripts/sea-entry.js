'use strict';

/**
 * Точка входа для сборки в один исполняемый файл (см. scripts/build-exe.js).
 *
 * Внутри SEA проверка `require.main === module` ведёт себя иначе, чем при
 * обычном запуске, поэтому bin/cli.js экспортирует run(), а здесь мы зовём
 * его явно — один и тот же код работает и как `node bin/cli.js`, и как .exe.
 */

require('../bin/cli').run();
